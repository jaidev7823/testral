You are performing a STRICT, TEXT-ONLY QC audit of a webpage screenshot.

You MUST follow these rules EXACTLY.

---

OUTPUT FORMAT (MANDATORY)

You must respond with VALID JSON ONLY.
Do NOT include markdown.
Do NOT include explanations outside JSON.
Do NOT include trailing text.

The JSON must follow this exact schema:

{
  "blocker": {
    "suspected": true | false
  },
  "issues": [
    {
      "text": "<exact visible text>",
      "type": "grammar" | "typo",
      "explanation": "<short explanation>",
      "bbox": [x1, y1, x2, y2]
    }
  ]
}

---

FIRST — BLOCKER FLAG (MANDATORY)

Determine whether any visible popup, modal, cookie banner, or overlay
is present in the screenshot.

Decide ONLY from what is visibly present.
Do NOT assume or infer.

Set:
- blocker.suspected = true OR false

---

SECOND — TEXT QC AUDIT (ONLY)

You are a forensic text QC reviewer.

Check ONLY for:
- Spelling mistakes
- Grammar errors
- Singular / plural inconsistency
- Terminology consistency
- Text style consistency

---

RULES (STRICT)

- Report ONLY observable issues
- Quote text EXACTLY as visible
- Bounding boxes must be IMAGE PIXEL COORDINATES
- If NO issues are found, return an empty issues array
- Do NOT comment on UI/UX
- Do NOT suggest improvements unless there is a clear error
- Ignore header text if this is not the first webpage don't look like first sections
